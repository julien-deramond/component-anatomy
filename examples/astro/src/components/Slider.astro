---
export interface Props {
  value?: number;
  min?: number;
  max?: number;
}
const { value = 40, min = 0, max = 100 } = Astro.props;
const pct = ((value - min) / (max - min)) * 100;
---

<div data-part="root" class="slider" role="none">
  <div data-part="track" class="slider-track">
    <div data-part="range" class="slider-range" style={`width: ${pct}%`}></div>
    <div
      data-part="thumb"
      class="slider-thumb"
      style={`left: ${pct}%`}
      role="slider"
      aria-valuemin={min}
      aria-valuemax={max}
      aria-valuenow={value}
      tabindex="0"
    ></div>
  </div>
  <div data-part="output" class="slider-output" aria-live="polite">
    {value}
  </div>
</div>

<style>
  .slider {
    width: 100%;
    max-width: 280px;
    user-select: none;
  }

  .slider-track {
    position: relative;
    height: 6px;
    background: var(--color-line);
    border-radius: 99px;
    margin-bottom: 1.2rem;
  }

  .slider-range {
    position: absolute;
    top: 0; left: 0; bottom: 0;
    background: var(--color-primary);
    border-radius: 99px;
    pointer-events: none;
  }

  .slider-thumb {
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--color-bg);
    border: 2px solid var(--color-primary);
    box-shadow: 0 1px 4px rgb(0 0 0 / 0.4);
    cursor: grab;
    transition: border-color 120ms;
  }

  .slider-thumb:focus-visible {
    outline: 2px solid var(--color-primary-lift);
    outline-offset: 2px;
  }

  .slider-output {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-primary);
    text-align: right;
    font-variant-numeric: tabular-nums;
  }
</style>
